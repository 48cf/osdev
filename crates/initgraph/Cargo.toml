[package]
name = "initgraph"
version = "0.1.0"
edition = "2024"

[dependencies]
initgraph-proc = { path = "../initgraph-proc" }
intrusive-collections = { version = "0.9.7", features = ["nightly"] }
spin = "0.10.0"
